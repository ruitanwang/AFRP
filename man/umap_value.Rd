% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_functions.R
\name{umap_value}
\alias{umap_value}
\title{UMAP基因表达热图可视化}
\usage{
umap_value(
  seurat_obj = seuobj,
  plotgene = "CHCHD10",
  plot_title = "CHCHD10",
  plot_title_color = "black",
  point_size = 0.2,
  point_alpha = 0.5,
  legend_title = "express",
  color_palette = c("#f4d58b", "#800025"),
  zero_point_color = "gray80",
  subcol,
  subneed,
  if_label_cell_num = TRUE,
  x_extend = c(0.15, 0.1),
  y_extend = c(0.15, 0.1)
)
}
\arguments{
\item{seurat_obj}{Seurat对象（必需），需已完成标准预处理（含UMAP降维）}

\item{plotgene}{目标基因名（默认："CHCHD10"），必须存在于\code{SCT}assay的\code{data}层}

\item{plot_title}{图形主标题（默认与基因名相同）}

\item{plot_title_color}{标题颜色（默认："black"）}

\item{point_size}{UMAP点大小（默认：0.2），推荐范围0.1-1.0}

\item{point_alpha}{点透明度（默认：0.5），范围0（全透）~1（不透明）}

\item{legend_title}{图例标题（默认："express"）}

\item{color_palette}{非零表达点色阶（默认：c("#f4d58b","#800025")），需包含2个及以上颜色值}

\item{zero_point_color}{零表达点颜色（默认："gray80"）}

\item{subcol}{元数据列名（可选），用于筛选细胞亚群}

\item{subneed}{亚群标识（可选），需与\code{subcol}指定的列中值匹配}

\item{if_label_cell_num}{是否标注细胞数量（默认：TRUE），显示于左下角}

\item{x_extend}{X轴扩展比例（默认：c(0.15,0.1)），控制绘图边界}

\item{y_extend}{Y轴扩展比例（默认：c(0.15,0.1)），控制绘图边界}
}
\value{
返回ggplot2对象，包含以下特性：
\itemize{
\item 双图层点图：零表达点（灰色）与非零表达点（渐变色）分层渲染
\item 固定纵横比（\code{coord_fixed()}）保持UMAP空间结构
\item 学术级主题（无边框+黑色轴线+加粗文本）
}
}
\description{
在单细胞转录组UMAP降维空间中可视化特定基因的表达分布，通过双色渐变系统（零值/非零值分层着色）精确展示基因表达的空间定位特征。支持亚群数据筛选与细胞数量标注。
}
